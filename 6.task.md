---
title: Sanear la contradicción sobre `hash.N` de un endpoint layer
status: done
created_at: 2026-08-29T00:00:00Z
updated_at: 2026-08-29T23:54:18Z
parent: 5
---

# Sanear la contradicción sobre `hash.N` de un endpoint layer

Gana `bilink.md`: el `hash.N` de un endpoint layer es copia del `hash.N` estructural adyacente, nunca el hash del archivo `.bilink`. Lo confirman el disco y el código.

Se corrige del lado de las specs que están mal —`chain.md:47`, `check.md` § "Endpoint layer" paso 4, `bilinker/architecture.md:57` y el diagrama de 57-63—, no del código, que ya hace lo correcto. Es el único caso de la épica donde la spec se alinea al código y no al revés.

## Resuelta desde la task `o`

Se adelantó del sprint 3 al 1: verificar los bilinks nuevos sobre `check_layer` y `accept_layer` puso la contradicción adelante, y dejar dos copias corregidas y dos sin corregir habría dejado las specs peleadas entre sí.

Los cuatro puntos del enunciado, más uno que no estaba: `commands/accept.md:19` decía "SHA-256 del fragmento (o del `.bilink` adyacente para layer)". `accept.rs::compute_hash_and_commit`, rama `LinkEndpoint::Layer`, copia `adj_bl.structural_hash()`.

También se corrigió el diagrama de `chain.md` § "Propagación reactiva", que atribuía la propagación a que el archivo del nodo cambia, y el paso 4 de `check.md` § "Endpoint layer", que quedó alineado con `check_layer` completo —TODO contra BROKEN según haya o no `hash.N`, y PENDING cuando el vecino nunca se aceptó.
